@model IEnumerable<Employee>

@{
    //Layout = "~/Views/Shared/_Layout.cshtml"; this line is no longer required due to the viewstart file
    ViewBag.Title = "Employee List";
}

@* <table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Department</th>
        </tr>
    </thead>
            
    <tbody>
    @foreach (var employee in Model)
    {
        <tr>
            <td>@employee.Id</td>
            <td>@employee.Name</td>
            <td>@employee.Department</td>
        </tr>
    }
</tbody>
</table> *@

<div class="card-deck">
    @foreach(var emplyee in Model)
    {
        <div class="card m-3">
            <div class="card-header">
                <h3>@emplyee.Name</h3>
            </div>

            <img class="card-img-top" src="~/images/car1.jpg" alt="car 1" 
                asp-append-version="true">
            <div class="card-footer text-center">

                <a asp-controller="home" asp-action="details" asp-route-id="@emplyee.Id" 
                    class="btn btn-primary">View</a>
                
                <a href="#" class="btn btn-primary">Edit</a>
                <a href="#" class="btn btn-danger">Delete</a>
            </div>

        </div>
    }
</div>

